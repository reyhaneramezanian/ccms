import Tabs from '@/components/tabs/tabs';
import { styled } from '@mui/system';
import { Box } from '@mui/material';
export const SecurityProfileTabs = styled(Tabs)({
    margin: '32px 0'
});
export const warper = styled(Box)({
    margin: '0 0 0 20px'
});
